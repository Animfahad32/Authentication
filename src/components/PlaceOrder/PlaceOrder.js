import React from 'react';

const PlaceOrder = () => {
    return (
        <div>
            <h2>Placed orders</h2>
            <h4>Now Give us payment if you're logged in</h4>
        </div>
    );
};

export default PlaceOrder;